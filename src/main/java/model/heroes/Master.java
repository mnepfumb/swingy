package model.heroes;

import model.HeroStats;
import model.artifacts.Artifact;

public class Master extends Hero 
{
	public Master( String player, HeroStats heroStats, Artifact artifact )
	{
		super( player, heroStats, artifact );
	}
}
